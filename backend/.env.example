# ============================================================
# 密钥配置说明(本文件仅作文档,无需复制为 .env)
# ============================================================
#
# 本项目直接读取 Windows 系统环境变量,不使用 .env 文件。
# 请确保以下变量已在「系统属性 → 环境变量 → 用户变量」中配置:
#
#   DEEPSEEK_API_KEY      DeepSeek 的 API Key
#   BAIDU_API_KEY         百度云 OCR 的 API Key
#   BAIDU_Secret_Key      百度云 OCR 的 Secret Key
#
# 程序启动时会自动从环境变量 / 注册表读取,代码中不存储任何密钥值。
